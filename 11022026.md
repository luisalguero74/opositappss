# Recordatorio 11022026 (resumen del día)

Fecha: 11/02/2026

## Hecho hoy

- Manual actualizado con el nuevo sistema de tiers + becas:
  - `MANUAL_ADMINISTRADOR.md`: tiers (Free=equivalente a Premium), becas ECAP por grupo en `/admin/allowed-phones`, excepciones Auto/Incluir/Excluir, activación del paywall por 2 toggles.
  - `ACTIVACION_SUSCRIPCION.md`: explicado paywall por 2 toggles (`/admin/monetization` + `/admin/monetization-free`), notas de trial, y límites Basic.

- Producción desplegada en Vercel:
  - `npx vercel --prod` completado y alias aplicado a `https://www.opositapp.site`.

- Error interno en “Teléfonos permitidos” (producción) arreglado:
  - Causa: faltaban columnas en BD de producción (`AllowedPhoneNumber.scholarshipOverride`, `Subscription.source`, `Subscription.label`).
  - Acción: aplicado SQL de la migración directamente:
    - `prisma/migrations/20260210120000_subscription_scholarships/migration.sql`
  - Resultado: columnas presentes y el listado de teléfonos NO estaba vacío (había ~167 registros).

- Migraciones Prisma por pooler (PgBouncer) estabilizadas (local tooling):
  - Ajustado `scripts/get-db-url-for-prisma-migrate.mjs` para añadir `statement_cache_size=0` además de `pgbouncer=true`/`connection_limit=1`.
  - Motivo: evitar `prepared statement "s1" already exists` con pooler.
  - Nota: aunque la migración se aplicó vía SQL en prod, este ajuste evita el fallo para futuros `prisma migrate deploy` vía pooler.

- Migraciones Prisma “de forma definitiva” (restaurado tracking en prod):
  - Problema raíz: BD de producción no tenía tabla `_prisma_migrations` (probablemente creada en su día con `prisma db push`).
  - La conexión directa `db.<project>.supabase.co:5432` no era viable desde esta red (host IPv6-only + red sin IPv6).
  - Solución aplicada: usar Supabase pooler en modo *session* (`aws-1-eu-west-1.pooler.supabase.com:5432`) para operaciones de CLI.
    - `scripts/get-db-url-for-prisma-migrate.mjs`: fuerza pooler `:5432` si venimos de `:6543`.
    - `scripts/get-db-url-for-migrations.mjs`: deriva a pooler session `:5432` en vez de intentar “direct db”.
    - `scripts/prisma-baseline-prod.mjs`: permite pooler `:5432` (sigue rechazando `:6543`).
  - Resultado: baseline aplicado (8 migraciones marcadas como applied), `_prisma_migrations` creada, y `npx prisma migrate status` reporta “Database schema is up to date!”.

- IPv6 (router O2 Smart WiFi 6 Go):
  - Detectado que la red local no tenía ruta IPv6 (en Mac: `IPv6 Router: none` y `ping6` sin ruta).
  - El host directo de Supabase resolvía solo AAAA (IPv6). Se pidió a O2 comprobar si IPv6/PD está habilitado en la línea.

- Cloudflare WARP (pruebas):
  - WARP conecta y asigna IPv6 al túnel (`utun3`) y `ping6` a `2606:4700:4700::1111` funciona.
  - Pero `dns.lookup` (getaddrinfo, usado por Node/psql) sigue devolviendo `ENOTFOUND` para `db.*.supabase.co` (host AAAA-only).
  - `dns.resolve6` sí resuelve el AAAA: la limitación es del resolver del sistema / detección de IPv6 “usable”.
  - Conclusión: esperar a IPv6 real por parte de O2 (DHCPv6-PD) o mantener pooler session `:5432`.

- Sello/Badge ECAP del dashboard restaurado:
  - Identificado en `src/components/LicenseBadge.tsx` (usa `public/logos/ecap.png`).
  - Reinsertado en `app/dashboard/page.tsx`.
  - Redeploy a producción realizado.

## Observaciones

- Consola del navegador:
  - Warning AdSense: “head tag doesn't support data-nscript attribute …” (warning, no bloqueante en principio).
  - Warnings de fuentes `woff2` precargadas no usadas: warnings de rendimiento (no bloquean funcionalidad).

- Nota de red:
  - Si O2 habilita IPv6 (DHCPv6-PD), podría volver a intentarse la conexión directa a Supabase `db.*:5432`.
  - No es requisito para migraciones: queda estabilizado con pooler session `:5432`.

## Puntos de verificación rápidos

- `/admin/allowed-phones` carga sin “Error interno” y muestra columnas de Grupo/Beca.
- Un teléfono con Grupo que incluya “ECAP” genera/activa beca y muestra la etiqueta “Suscripción gratuita alumnos ECAP” en `/dashboard/subscription`.
- Basic respeta límites solo cuando el paywall está activo (2 toggles ON).
- Prisma en prod:
  - `_prisma_migrations` existe y lista 8 migraciones.
  - `npx prisma migrate status` devuelve “Database schema is up to date!”.
